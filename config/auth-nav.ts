import { AuthNavConfig } from "@/types"

export const authNavConfig: AuthNavConfig = {
  authNav: [
    {
      title: "Login",
      href: "/login"
    },
    {
      title: "Sign Up",
      href: "/register"
    },
  ]
}
